- @release_builds.each do |release_build|
  %p
    %a{:href => "itms-services://?action=download-manifest&amp;url=#{show_build_manifest_url(@app_name, release_build, :format => :plist)}"}=release_build


    